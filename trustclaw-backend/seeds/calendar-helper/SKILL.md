# Calendar Helper

Parse natural-language dates and calculate date differences.

## Usage
```bash
./calendar.py "next friday"
./calendar.py "2 weeks from now"
./calendar.py diff "2025-01-01" "2025-12-31"
```

## Safety
- Pure computation, no network access
- No calendar write access
- No data stored
