# Translator

Translate text between languages using the free LibreTranslate-compatible API.

## Usage
```bash
./translator.sh "Hello world" en es
./translator.sh "Bonjour" fr en
```

## Safety
- Uses free translation API (no key required)
- Read-only network requests
- No data stored locally
