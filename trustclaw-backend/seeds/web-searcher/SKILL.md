# Web Searcher

Search the web using DuckDuckGo's HTML interface (no API key needed).

## Usage
```bash
./search.sh "rust async tutorial"
./search.sh "best pizza in oslo" 5
```

## Safety
- Read-only HTTP requests
- No API key required
- No data stored
